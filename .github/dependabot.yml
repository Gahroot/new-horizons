version: 2
updates:
  # The package has no third-party runtime dependencies; keep the pinned CI actions current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
